Before run the code, you need install libraries bellow (for Windows): 
1. Check if U have a python. If don't download it from: python.org/download/
2. BeautifulSoup: pip install beautifulsoup4
3. Unittest is a standard module, which you don't need to install
4. Run the test: python -m test_name